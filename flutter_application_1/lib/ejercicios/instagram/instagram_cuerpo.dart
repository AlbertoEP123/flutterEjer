import 'package:flutter/material.dart';

class InstagramCuerpo extends StatelessWidget {
  const InstagramCuerpo({super.key});


  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      
      
  
    );
  }
}